#include <stdio.h>
#include <stdbool.h>
/*
int hashFunction(int key);


int insertKey  = 0;
int hIndex = hashFunction(insertKey);
bool found = false;

while(HT[hIndex].key != emptyKey && !found){
    if(HT[hIndex].key == insertKey){
        found = true;
    }else{
        hIndex = (hIndex + 1) % HTSize;
    }
}

if(found){
    Error("Duplicate Key");
}else{
    HT[hIndex] = newItem
}
*/